eureka:
  client:
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: WELCOME-API
  boot:
    admin:
      client:
        url: http://localhost:1111

server:
  port: 8081